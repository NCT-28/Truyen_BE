version: '2'
services:
  truyen-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/truyen/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=truyen
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
